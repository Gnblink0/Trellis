# Backend API URL (required for connecting to server)
# For simulator: use http://localhost:3001
# For real device: use your machine's LAN IP with port, e.g. http://192.168.1.100:3001
# The :3001 part is required (Express listens on 3001, not 80).
EXPO_PUBLIC_API_URL=http://localhost:3001
